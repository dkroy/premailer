Make a npm project with grunt build process. 

/dist (has premailer.js)

/demos (Has interactive email builder)


Add to npm server.

Build Readme


branches:

master

gh-pages

when someone wants to add a feature they can just create feature-newfeaturename

Add some versioning
